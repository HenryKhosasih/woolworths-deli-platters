/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn0.woolworths.media",
      "assets.woolworths.com.au",
      "via.placeholder.com",
    ],
  },
};

module.exports = nextConfig;
